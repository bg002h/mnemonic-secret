You are an INDEPENDENT R0 reviewer (round 0, FIDELITY lens) of an implementation plan in the mnemonic-secret repository (`/scratch/code/shibboleth/mnemonic-secret`, master at `36d314daa98cb1a6d9212b47d1f44cfc04be47b8`). The artifact is `design/IMPLEMENTATION_PLAN_ms_hashlock_H1.md`. The spec it implements is `design/SPEC_ms_hashlock.md` (R0 GREEN under lens-closure at `d4d6771`; three lenses + a fold verification, all persisted in `design/agent-reports/ms-hashlock-spec-R0-*`). The spec's rulings are the OPERATOR'S (brainstorm L1–L23 in mnemonic-engrave `design/BRAINSTORM_hashlock_phrase.md`); three controller defaults are labelled in the spec and await the operator — do not re-litigate any of them.

ONE QUESTION: does the plan, executed step by step by an implementer who has read only the plan and the spec, produce EXACTLY what the spec specifies — no requirement dropped, none altered, nothing invented — and can each step be executed as written?

Read-only. Copy the repo with `cp -r` if you run anything against a modified tree. Commit nothing. Do NOT spawn sub-agents. Read no `.jsonl` file. Toolchain: the repo's pinned Rust (`rust-toolchain.toml`, 1.85.0) via `~/.cargo/bin`; `TMPDIR=/scratch/code/shibboleth/.tmp`.

## Already machine-checked — spend your budget past these

The plan's code was BUILT AND RUN before you were dispatched, by `scripts/plan-build-gate-ms.sh` (its header states what it does and does not cover):

- Every ```rust block anchored on a NEW file was extracted and compiled against a scratch copy of the tree with the FRAGMENTS (edits to existing files) applied by `scripts/plan-handwire-ms-hashlock.py`.
- `The gate on this SHA (run 11, .tmp/ms-gate-r11.log): workspace build --locked; 64 hashlock tests run, 64 passed; clippy --all-targets -D warnings clean; rustfmt clean; codeword distance entr/hash = 17; the downgrade row (pre-H1 ms on the wired binary's plate string) exits 2 with `reserved-prefix byte was 0x03`.`
- The gate does NOT cover: fragment TEXT beyond what the hand-wire script applies (if the plan's prose describes a fragment differently from the script's string, the script is what ran); the toolkit manual chapter; `.github/workflows` edits; H0 in the other two repos.

So do not report compile errors, test failures, clippy or fmt findings on the gated code — they cannot exist at this SHA. What the gate cannot see is your whole job.

## Verify

1. **Traceability, spec → plan.** For every normative sentence of spec §1–§12 (skip §13 out-of-scope and §14 citations), name the task and step that implements it, or DROPPED. Pay special attention to: §1's three rules (accept set, tag/kind mismatch refusal on decode AND encode, blocklist) and the length-row doors; §2's four functions and the two dependency spellings; §3's `Zeroizing` field, the LOUD/SILENT edit-site split, the catch-all count test; §4.1's `--random`/`--out`/zero-source/`--hex`-warning rules; §4.3's normalising predicate, byte-verbatim reader, printable range, check ORDER (shape before cap), 64-hex either case; §4.4's polarity, `--json` replacing the record line, omission rules, lowercase hex, the `me sysw pack` spelling; §5's per-verb table incl. `repair`; §6's six parts and three gates; §7's copy verbatim (compare strings character for character against the plan's Rust literals); §8's rows (each with its door and error), the reproduction test's four properties (literal constants, three-way captured stdout, fails-if-absent, run-by-name), the lockstep set; §9's five MIGRATION items and H0; §10's release order; §11's test list; §12's eight acceptance items. A dropped or altered requirement is Important; one that changes X, H, a refusal or a persistence rule is Critical.
2. **Traceability, plan → spec.** Anything the plan builds that the spec does not ask for (invented flags, extra copy, an error the spec does not name, a behaviour on a source the spec leaves alone) is a finding: Important if operator-visible.
3. **Executability.** For each task: are the file paths and line ranges real at this SHA (spot-check five)? Does each "Run:" step's Expected line describe what would actually happen (spot-check the RED steps: would the named test really fail for the named reason before its implementation lands)? Are the fragment descriptions in prose consistent with the hand-wire script's strings (the script is the truth; a prose description that says something different is a Minor unless it would mislead an implementer into a different edit — then Important)?
4. **Interface consistency across tasks.** Every type, function and constant used in a later task is defined in an earlier one with the same signature (`Payload::Preimage(Zeroizing<[u8; 32]>)`, `PayloadKind::single_tag`, `hashlock::preimage_random -> Result<...>`, `CliError::Usage`, `emit_preimage(&[u8; 32], bool) -> Result<u8>`, `write_artifact_create_new`, `looks_like_ms1`, the `Result`-returning `payload_entropy_and_language`). Name any mismatch.
5. **The corpus's `…` cells.** Task 4's JSON leaves derivation rows for the implementer to fill from two external tools with a `provenance` field. Judge whether the plan's instruction is sufficient to produce byte-identical rows on host and device, and whether anything else in the plan is a placeholder in disguise (grep for TBD/TODO/"similar to"/"appropriate"/"handle edge cases" and for any step with no code where code is needed).
6. **H0.** The plan says H0 (the fork's classifier guard, `me`'s record-validator guard) ships BEFORE the 0.18.0 release and Task 11 gates on it. Is that gate concrete enough to STOP a release (what exactly is checked, by what command), or is it prose?

Do NOT: audit the spec (closed); mutation-test the tests (the tests lens owns that); propose design changes (a design objection to a spec'd behaviour is a note for the operator, filed separately).

## Severity
Critical = an executed plan yields a wrong X/H/record, loses a preimage, or ships without a spec'd refusal or persistence rule; or a step that cannot be executed and blocks the rest. Important = a dropped/altered requirement, an interface mismatch, an unexecutable step with a workaround, an invented behaviour. Minor/Nit = wording, prose/script drift that misleads no one. Number C-n / I-n / M-n / N-n; quote the plan line and the spec line. Do not pad.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-secret/design/agent-reports/ms-hashlock-H1-plan-R0-r0-fidelity.md` (create; must not exist): the two traceability tables in full; findings by severity with quotes; closing counts C/I/M/N. Return a two-line summary plus the path.
