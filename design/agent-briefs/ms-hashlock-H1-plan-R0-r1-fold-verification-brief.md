You are the INDEPENDENT fold-verification reviewer (sonnet tier, targeted) for round 1 of the H1 plan's R0 in mnemonic-secret (`/scratch/code/shibboleth/mnemonic-secret`). Round 0 = two lenses, persisted verbatim: fidelity (opus) at `95f417c` — `design/agent-reports/ms-hashlock-H1-plan-R0-r0-fidelity.md`, 2C/10I/9M/3N — and tests (sonnet) at `2f4a93b` — `design/agent-reports/ms-hashlock-H1-plan-R0-r0-tests.md`, 0C/4I/3M/1N. The fold is ONE commit, `3592532`, over the gate-green draft `36d314d`; its message lists what changed per finding. The fold was RE-GATED before you were dispatched (a fold is authorship and re-earns the gate): `run 13: 75/75 hashlock tests passed, clippy -D warnings and cargo fmt --check clean, codeword distance entr/hash = 17, downgrade row (pre-H1 ms d4d6771 on the wired binary's plate string) exit 2 with 'reserved-prefix byte was 0x03', MS-GATE EXIT: 0`.

ONE QUESTION: did the fold address every Critical and Important from both reports — FIXED / PARTIAL / NOT FIXED / DECLINED-with-reason, one line each — without introducing a contradiction or a false claim of its own?

Read-only; commit nothing; no sub-agents; read no `.jsonl`. You MAY run the plan's code: `cp -r` the repo to `/scratch/code/shibboleth/.tmp/fold-verify-ms`, and inside the copy run `scripts/plan-build-gate-ms.sh design/IMPLEMENTATION_PLAN_ms_hashlock_H1.md` once (`TMPDIR=/scratch/code/shibboleth/.tmp`, `PATH=$HOME/.cargo/bin:$PATH`; ends with `MS-GATE EXIT: 0`); the wired tree is then at `$TMPDIR/plan-build-gate-ms`. You are the only process allowed to run the gate right now.

## Already settled — do not re-derive
- The two reports are the specification for this round; read them first.
- The gate proved the fold's code builds, its tests pass, clippy and fmt are clean, the codeword distance is 17 and the downgrade row exits 2. Do not report compile/test/lint findings on the gated code.
- Two items in the fold are CONTROLLER DEFAULTS awaiting the operator, labelled in the plan: `--hashlock-phrase -` refused (fidelity I-10); the spec's three earlier defaults are unchanged. Note whether they are labelled; do not argue them.

## Verify
1. **The finding table.** For each C and I in both reports (2 + 10 + 4 = 16), the plan section that now carries the fix, quoted, and a verdict. A finding the fold message says it addressed but whose text you cannot find is NOT FIXED. Then the Minors and Nits, one line each.
2. **The two Criticals, executed.** On the wired scratch: (a) forge a `hash`-id single over a `0x00` payload (the test `tag_kind_mismatch_is_a_format_violation_on_decode_and_a_reason_on_inspect` shows how) and confirm `ms decode` exits 2 with the spec's wording and `ms inspect` names `tag-kind-mismatch` and says would NOT decode; (b) confirm the `From<ms_codec::Error>` arms exist in the wired `crates/ms-cli/src/error.rs` and that no path reaches the `unhandled ms_codec::Error variant` catch-all for the three new variants.
3. **New contradictions.** The fold rewrote: Global Constraints (a new default), the File Structure table, Task 1 (both Cargo bumps), Task 2 Step 5 (the forward-compat loop), Task 4 (the corpus rows — now literal, eleven of them — the loader test, the CI spelling), Task 5 Step 2's Expected, Task 7's `--hex` parsing and `create_new`, Task 8's inspect check and split.rs, Task 9's new tests, Task 10's lint command, Task 11's H0 command, the self-review. Read those as a hostile implementer: does any sentence now contradict another, or the hand-wire script's strings (the script is what ran)?
4. **The corpus.** Every derivation row now carries a literal phrase and four hex values with a `provenance` line. Spot-check THREE rows (not the anchor) with `python3 hashlib` and one hardened X with `openssl kdf`; confirm the loader test `corpus_rows_are_filled_and_re_derive` actually reads the file and would fail on a placeholder (put a `…` back in a copy and run it).
5. **The tests-lens Importants, executed.** I-1: `case_is_bytes_too` and the mixed-case corpus row exist and fail if the phrase is lowercased before hashing (mutate `preimage_hardened` to lowercase its input in the scratch). I-3: the structural "three lines" check fails when an extra line is printed by `emit_preimage` (mutate). I-4: the hex-looking-longer-phrase test exists and passes.

## Severity
A C/I marked FIXED in the message but not fixed in the text = Critical. A new contradiction between two normative sentences, or between the plan and the hand-wire script = Important. Wording = Minor/Nit. A clean round closes R0 for this plan (lens-closure: fidelity, tests/mutation, fold-verification).

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-secret/design/agent-reports/ms-hashlock-H1-plan-R0-r1-fold-verification.md` (create; must not exist): the finding table (16 C/I rows, then M/N), the executed checks with output, the corpus spot-checks, closing counts and a plain GREEN / NOT GREEN. Return a two-line summary plus the path.
