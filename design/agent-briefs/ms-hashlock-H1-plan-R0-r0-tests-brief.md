You are an INDEPENDENT R0 reviewer (round 0, TESTS lens — can every test fail?) of an implementation plan in the mnemonic-secret repository (`/scratch/code/shibboleth/mnemonic-secret`, master at `36d314daa98cb1a6d9212b47d1f44cfc04be47b8`). The artifact is `design/IMPLEMENTATION_PLAN_ms_hashlock_H1.md`, implementing `design/SPEC_ms_hashlock.md` (R0 GREEN). The spec's rulings are the operator's; do not re-litigate them, and do not re-audit the spec.

ONE QUESTION: for every guarantee the plan's tests claim to protect, would the plan's own named MUTATION actually make that test fail — and which spec'd guarantee has NO test that can fail on it?

Read-only. **You will run the plan's code**: it is buildable by the gate. Do `cp -r /scratch/code/shibboleth/mnemonic-secret /scratch/code/shibboleth/.tmp/tests-lens-ms` and inside the copy run `scripts/plan-build-gate-ms.sh design/IMPLEMENTATION_PLAN_ms_hashlock_H1.md` ONCE to obtain the wired scratch tree at `$TMPDIR/plan-build-gate-ms` (set `TMPDIR=/scratch/code/shibboleth/.tmp` and `PATH=$HOME/.cargo/bin:$PATH`; the run takes a few minutes and ends with `MS-GATE EXIT: 0`). Mutate THAT scratch tree (never the repo), rebuild with `cargo build --workspace --all-targets` and run the relevant tests with `cargo nextest run --workspace -E 'test(<name>)'`. Commit nothing. No sub-agents. Read no `.jsonl`.

## Already machine-checked — do not re-derive
- `The gate on this SHA (run 11, .tmp/ms-gate-r11.log): workspace build --locked; 64 hashlock tests run, 64 passed; clippy --all-targets -D warnings clean; rustfmt clean; codeword distance entr/hash = 17; the downgrade row (pre-H1 ms on the wired binary's plate string) exits 2 with `reserved-prefix byte was 0x03`.`
- The gate's own two mechanical proofs are already run: the codeword distance (17) and the downgrade row (a `0x03` single fed to the pre-H1 `ms` exits 2 with the reserved-prefix text).

## Verify

1. **Mutation table, plan-declared.** Every test in the plan's new test files carries a `MUTATION:` line (or a doc sentence naming one). For each: apply exactly that mutation to the scratch tree, rebuild, run the named test. Record PASS-STILL (a false claim — the test cannot fail on its own mutation) or FAILS-AS-CLAIMED, with the failing assertion's text. A PASS-STILL on a funds/preimage guarantee is Critical; on any other guarantee Important.
2. **Mutation table, yours.** Add the mutations the plan did NOT name, from the spec's guarantees: swap the two methods; iterations 10_000; salt one byte off; dkLen 16; `digest` hashing the phrase instead of X; `strip_one_trailing_newline` stripping two; the printable range as `is_ascii()`; the cap at 99 and 101; `looks_like_ms1` without the case fold; the 64-hex guard testing 63; the ms1-shape check AFTER the cap; `PREIMAGE_PREFIX = 0x01`; the length check after `try_into`; `TagKindMismatch` never raised on decode (accept set only); `TagKindMismatch` never raised on encode; `RESERVED_ID_BLOCKLIST` without `hash`; `--random` gated on `--out || --json`; `--random`'s `--out` truncating; `--out` suppressing stdout; `--method` silently ignored with `--hex`; `--json` keeping `method` for a supplied X; uppercase hex in `hash:`; each of the four `unreachable!` arms left in place (one at a time); the derive/verify refusal placed after `payload_entropy_and_language`; `emit_preimage` printing words; `inspect` still pushing `unknown-tag`; the repro test built from the crate's constants (i.e. delete `constants_equal_the_literals` and use `HASHLOCK_SALT` in the script); the `#[cfg(test)]` module count pin. For each: which plan test catches it, or NONE.
3. **False-PASS designs on the three structural tests**: the reproduction test (does it still pass if `python3` is a shim printing the expected hex? if `openssl` is absent but python present? — the spec says it must FAIL), the catch-all count test (does it count the right pattern? could a `_ => todo!()` slip past?), and the `Zeroizing` type pin (the plan says the compiler enforces it — does any test exist at all, or is it prose?).
4. **Corpus sufficiency for the Go port.** The plan's corpus JSON: could a behaviour-faithful Go port that trims a trailing space, folds case on the phrase, or accepts a tab pass every row it vendors? Name the drift each listed row cannot see.
5. **The RED steps.** For five tasks, check that Step 1's test really fails for the reason Step 2's Expected line states, at that point in the sequence (before that task's implementation). A test that already passes on the unmodified tree, or fails for a different reason, is a Minor (the plan's TDD claim is false at that step).

Do NOT: check spec traceability (the fidelity lens owns it); propose design changes; re-run the gate's proofs.

## Severity
A test that cannot fail on a funds/preimage guarantee = Critical; on another guarantee = Important; a spec'd guarantee with no test = Important; a RED step that is not red = Minor; wording = Nit. Number C-n / I-n / M-n / N-n; every finding cites the test name, the mutation applied (as a diff or a one-line description), and the observed result.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-secret/design/agent-reports/ms-hashlock-H1-plan-R0-r0-tests.md` (create; must not exist): both mutation tables in full (mutation → test → result), the three false-PASS analyses, the Go-port drift list, the RED-step checks, closing counts. Return a two-line summary plus the path.
