You are the INDEPENDENT adversarial execution reviewer (opus tier) for the mnemonic-secret hashlock stage H1 implementation: branch `hashlock-h1`, tip `a150ba7`, checked out at `/scratch/code/shibboleth/ms-worktrees/hashlock-h1` (base: master `4dbff0b`). The plan is `design/IMPLEMENTATION_PLAN_ms_hashlock_H1.md` (R0 GREEN), the spec `design/SPEC_ms_hashlock.md`, the implementer's own report `design/agent-reports/ms-hashlock-H1-implementation-report.md` on the branch (read it; it lists eight deviations D1-D8).

ONE QUESTION: over the WHOLE diff `git diff 4dbff0b..a150ba7`, does the shipped code do what the spec guarantees — and where can you construct an input that makes it give a WRONG RESULT, print a preimage where the spec says never, accept what the spec refuses, or pass a test that cannot fail?

Read-only on the branch and on master; commit nothing; no sub-agents; never read any `.jsonl`. Build and RUN freely in your own detached worktree: `git -C /scratch/code/shibboleth/mnemonic-secret worktree add --detach /scratch/code/shibboleth/ms-worktrees/h1-review a150ba7`; every cargo command with `PATH=$HOME/.cargo/bin:$PATH TMPDIR=/scratch/code/shibboleth/.tmp CARGO_TARGET_DIR=/scratch/code/shibboleth/mnemonic-secret/target`. Mutate only in that worktree, revert after each mutation, remove the worktree when done (`git worktree remove --force`).

## Already settled — do not re-derive
- The controller re-ran the gates on `a150ba7`: `cargo nextest run --workspace --locked` 554/554, `cargo test --workspace --locked` 555 passed / 0 failed, clippy `-D warnings` clean, `cargo fmt --check` clean. Do not report compile/lint/fmt findings.
- The plan's R0 closed under fidelity, tests/mutation and fold-verification lenses; the spec's R0 likewise. Do not re-review the plan or the spec; review the CODE against the SPEC, and treat every plan-vs-code deviation the implementer recorded as a claim to verify, not a fact.
- Four controller defaults are labelled in the plan and implemented as written (TagKindMismatch refusal; `--random` requires `--out`; H0 ordering — Task 11 is deliberately NOT run; `--hashlock-phrase -` refused). Verify they behave as labelled; do not argue them.
- Secret-handling defects (material reaching argv, stderr, shell history, a world-readable file) NEVER gate (operator ruling 2026-08-27): record them as Minor with a reproduction; do not rate them C/I.

## Construct counterexamples — an assessment without one is Minor at most
1. **The wire and the readers (spec §1, §5).** With the built `ms`: encode a preimage single, decode it, inspect it, `split`/`combine`/`verify`/`derive` on it; forge a `hash`-id over a `0x00` payload and an `entr`-id over `0x03`; a 74- and 76-character 0x03 string; uppercase; a share-set of a preimage (F-468 says split has no preimage SOURCE — but what does `ms split` do if handed a preimage plate as `--in`?). Every outcome must match the spec's table for that verb; quote stdout/stderr/exit.
2. **Derivation (spec §2, §8).** Re-derive three corpus rows (one hardened in `openssl kdf`) from the SHIPPED binary, not the test; a 64-hex phrase both cases; a 100- and 101-character phrase; a phrase with a leading/trailing space and with `\r\n` via `--hashlock-phrase-stdin`; non-ASCII; empty; an ms1-shaped phrase in every case form. The reader is byte-verbatim — prove it or break it.
3. **The argv guard (spec §6).** Every spelling the guard must catch for `--hashlock-phrase` (=, space, prefix abbreviations if clap allows them, `--hex` on argv, positional); what leaks to stderr on refusal; what `ms hashlock --help` prints.
4. **`--random` (spec §4.1, controller defaults).** `--random --out` to an existing file, to `/dev/null`, to a directory, to a path in a read-only dir; `--random --json` without `--out`; the created file's mode; what stdout carries when `--out` fails AFTER the preimage was generated (the only copy must not vanish at exit 0).
5. **Outputs (spec §4.4, §7).** The card's first line names the preimage; `hash:<64 lowercase hex>` on stdout; `--json` shape; the sha256 warning fires on the method not the phrase; the hardened <20-character warning.
6. **The deviations.** D2 (encode's check order): is the moved check still reached for every path the spec names, and does the hand-wire script now match the code? D4 (RED prediction) and D5 (GUI schema pin 55→67): is the pin a real count from `ms gui-schema`? D3 (`include_str!` of the corpus): does a placeholder in the corpus fail the BUILD or a TEST? D7 (toolkit manual filed): is the follow-up entry present with an owning phase?
7. **Tests that cannot fail.** Pick the five tests whose names promise the most (the never-words check, the argv guard census, the corpus loader, the tag/kind mismatch, the create_new refusal) and mutate the code they guard; a survivor is Critical if the guarantee is spec-normative.
8. **Rust-primary.** Nothing here may have been decided by the Go port; confirm no fork file is touched and the provenance/lockstep constants (`HASHLOCK_*`, codec `0.8.0`, cli `0.18.0`, the `=0.8.0` pin) are as the spec says.

## Severity
Critical: wrong result, a preimage printed where the spec says never, an accepted input the spec refuses, a test that cannot fail on a normative guarantee, a false claim in the implementation report. Important: a real defect or missing case. Minor: secret-handling, wording, records.

## Report (your final action)
Write `/scratch/code/shibboleth/mnemonic-secret/design/agent-reports/ms-hashlock-H1-post-impl-review.md` (in the MAIN checkout at that absolute path; create; must not exist): findings `### C-n / I-n / M-n / N-n — title` with the command, the verbatim output and the spec line it violates; a table of the eight deviations with your verdict each; the mutation table; closing counts and GREEN / NOT GREEN. Return a two-line summary plus the path.
