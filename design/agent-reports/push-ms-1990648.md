# Push report: mnemonic-secret master → 1990648

## Tip SHA
`1990648f9a95046b8b2b39a637af558138c719b5`

(one commit ahead of prior `origin/master` tip `cd0a60f`: "reports: ms release
push cd0a60f (four contexts, no bypass; tags ms-codec-v0.8.0 + ms-cli-v0.18.0;
man-release success, 7 assets) + H1 spec §12 acceptance (items 1-5 exact, 6 host
half, 7 software, 8 complete bar the manual; released musl binary re-checked)")

## CI run
- Repo: `bg002h/mnemonic-secret`
- Run id: `33941676246` (workflow `rust`), triggered by `git push origin
  master:refs/heads/ci/staging`
- Final run status: `completed` / conclusion `success` (confirmed both via
  `gh run watch 33941676246 --exit-status` exiting 0, and via a separate
  polling monitor on `gh run view --json status,conclusion`)

## Required contexts (`gh api .../commits/<full-sha>/check-runs`)
| context | conclusion |
| --- | --- |
| `test (ubuntu-latest)` | success |
| `clippy` | success |
| `test (ms-codec)` | success |
| `clippy (ms-codec)` | success |

All four required contexts: **SUCCESS**. (Every other check-run reported on
this commit — `test (macos-latest)`, `musl compile/test` x2, `fmt (pinned
1.95.0)`, `miri (mlock unsafe)`, `history purge`, `freebsd compile-gate`,
`test (release, ubuntu-latest, mlock einval)`, `g6 invariant
(cross-repo mlock.rs)` — also reported success; this commit touches no Cargo
files, and no vendor-freshness job appeared in the check-run list, matching
the brief's expectation that it would not run.)

## Push output (`git push origin master`, last lines verbatim)
```
To github.com:bg002h/mnemonic-secret.git
   cd0a60f..1990648  master -> master
```
Captured to `/scratch/code/shibboleth/.tmp/push-ms-1990648.log`.

## Bypass check
No "Bypassed rule violations" text in the push output above. **Not bypassed —
the push was accepted on the strength of the `ci/staging` SHA's passing
required contexts.**

## Post-push verification
- `git push origin --delete ci/staging` → `- [deleted]  ci/staging`
- `git fetch origin && git rev-parse origin/master` →
  `1990648f9a95046b8b2b39a637af558138c719b5` (equals local `master` tip)
- `git ls-remote origin refs/heads/ci/staging` → empty output (ref gone)

## Verdict

**SUCCESS**
