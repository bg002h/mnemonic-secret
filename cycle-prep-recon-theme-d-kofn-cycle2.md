# cycle-prep recon — 2026-06-02 — theme-d-kofn-cycle2 (ms K-of-N codex32 shares)

**Origin/master SHA at recon time:** `9d789b2` (mnemonic-secret; post mnem-v0.2 ship: ms-codec 0.3.0 + ms-cli 0.6.0 on master)
**Local branch:** `master`
**Sync state:** up-to-date (0 ahead / 0 behind origin/master)
**Untracked:** `cycle-prep-recon-mnem-language-hint.md` (prior cycle), this file
**codex32 dep:** `=0.1.0`, source re-verifiable at `~/.cargo/registry/src/*/codex32-0.1.0/`

Scope: **SPEC-scoped, no FOLLOWUP slug** (tier-`v0.2`). Substrate = `SPEC_ms_v0_1.md §5` + `MIGRATION.md` (the four invariants) + the authoritative corrected design in `design/agent-reports/ms-v0-2-spec-R0-review.md` (the combined-spec R0 that produced the mnem/K-of-N split). **Expectation: the pre-designed §5/MIGRATION.md contract is partly INVALIDATED (crypto), but the corrected design is already fully worked out in the prior R0; source substrate is clean + unbuilt as expected.**

---

## Per-slug verification

### K-of-N codex32 shares (the v0.2 share-encoding feature)

- **WHAT:** add K-of-N Shamir share encoding for `ms1` (BIP-93 codex32's defining capability; v0.1 hardcodes threshold=0 = unshared). New `ms-codec` `encode_shares`/`combine_shares` + a `Threshold` type; new `ms split`/`ms combine` CLI; the decoder learns the share path. The format's headline feature.

- **Citations (verified against `9d789b2` + codex32 0.1.0 + the prior R0):**

  - **Emit still hardcodes threshold-0 / index-`s`** — **ACCURATE.** `consts.rs:20` `pub const THRESHOLD_V01: u8 = b'0';`, `consts.rs:23` `pub const SHARE_INDEX_V01: u8 = b's';` (named consts now exist — the recon-args guess was right). `envelope.rs:187` `package()` → `from_seed("ms", 0, tag, Fe::S, …)` (literal threshold `0`, index `Fe::S`). The mnem cycle (0x02 prefix) left the threshold/index header **untouched** — orthogonal to the payload prefix byte. ✓
  - **Decoder hard-rejects threshold≠0 / index≠`s`** — **ACCURATE.** `envelope.rs:105-106` `if fields.threshold_byte != THRESHOLD_V01 → Error::ThresholdNotZero`; `:110-112` `if fields.share_index_byte != SHARE_INDEX_V01 → …`. So a v0.1 decoder rejects any K-of-N share — correct; relaxing this to *route* shares (threshold∈2..9) is the additive v0.2 change.
  - **No `encode_shares`/`combine_shares`/`Threshold`/`interpolate`/`derive_share` in ms-codec** — **ACCURATE** (K-of-N unbuilt). `Error::ThresholdNotZero` (error.rs:20) is the v0.1 *reject*, not share API. **Bonus groundwork:** `ms-cli/src/codex32_friendly.rs:31-77` ALREADY maps codex32-crate share errors (`InvalidThreshold`, `InvalidThresholdN`, `MismatchedThreshold`, `ThresholdNotPassed`) to friendly messages (pre-existing, likely for `repair`) — the K-of-N cycle reuses it for `combine`.
  - **ms-cli has no share/combine subcommand** — **ACCURATE.** `main.rs` enum = `Derive, Encode, Decode, Inspect, Verify, Repair` (6). New `Split`/`Share` + `Combine` needed.
  - **SPEC §5 invariant 1 (`0x01` = entr-share discriminator)** — **STRUCTURALLY-WRONG (crypto-invalidated).** `SPEC_ms_v0_1.md:216` (+ `MIGRATION.md:15`) reserve `0x01` as the v0.2 entr-share discriminator. **The prior R0 C2 (`ms-v0-2-spec-R0-review.md:36-46`) proved this incompatible with Shamir:** the type byte lives *inside* the codex32 payload (`package()` prepends it before `from_seed`); Lagrange interpolates per-payload-symbol (`codex32 lib.rs:290-293`), so a distributed share at index ≠ S has an *interpolated* first byte, not a stable `0x01`. Re-stamping it breaks the BCH checksum AND the polynomial → `combine` recovers the wrong secret. **Correct mechanism (BIP-93 native):** shares are identified by the **threshold char** (`0` vs `2..9`, `lib.rs:185-196`) + **share-index** (`s` vs other, `lib.rs:198`) — header fields, NOT a payload byte. `0x01` stays **unallocated**; the prefix byte remains the payload-KIND discriminator (`0x00`=entr, `0x02`=mnem).
  - **SPEC §5 invariant 2 (prefix-byte gating before id-grouping)** — **STRUCTURALLY-WRONG (crypto-invalidated).** `SPEC:218`/`MIGRATION:17` gate share-grouping on the prefix byte; distributed shares have no stable prefix byte. **Corrected:** dispatch on the **threshold field** — `threshold==0` → single-string path (entr `0x00` / mnem `0x02` via prefix), `threshold∈2..9` → share path, group by `id`. The secret-at-S payload is `[0x00][entropy]` (clean v0.1 shape → existing `discriminate()` reads it after recovery, unchanged).
  - **SPEC §5 invariant 3 (anti-collision `id`) + invariant 4 (`encode_shares` API)** — **ACCURATE / survive.** Inv-3 (refuse `id ∈ RESERVED_NOT_EMITTED`, re-roll random) stands. Inv-4 signature `encode_shares(tag, Threshold, payload_set) → Vec<String>` with `encode_shares(tag, ZERO, &[p])` **byte-identical** to `encode(tag,&p)` is sound + affirmed (`ms-v0-2-spec-R0:96`) — both reduce to `from_seed("ms",0,tag,Fe::S,[0x00]||payload)`.
  - **`MIGRATION.md` citation** — **ACCURATE** (initial recon false-alarmed: it's at repo **root** `./MIGRATION.md`, git-tracked, which is what SPEC's `../MIGRATION.md` resolves to — not `design/`). It carries the SAME four invariants → **must be amended in lockstep with §5** (inv 1+2).
  - **`envelope.rs` source-comments** — **ALREADY CORRECTED.** `envelope.rs:3` "When K-of-N share encoding ships in v0.2, only [uses] `parts.threshold`/`parts.share_index`"; `:92` discriminate doc lists `0x00`/`0x02` only — the stale `0x01`-share promise was already dropped (mnem-cycle M2 / earlier). No comment fix owed; **only §5 + MIGRATION.md text need the threshold-field rewrite.**
  - **codex32 0.1.0 K-of-N API** — **ACCURATE** (prior R0 traced + a working spike): `from_seed` threshold map k∈{0,2..9}, no 1, max 9 (`lib.rs:326-337`); `interpolate_at` (`lib.rs:217-308`) recombines (eager same-len/hrp/threshold/id checks `:238-252`; **lazy `RepeatedIndex` `:283` + count uses `shares.len()` not distinct-count `:230`** → `combine_shares` must pre-validate); `Parts::data()` byte-granular (`:399-428`); `bip_vector_3` (`:489-516`) is the round-trip oracle (defining set `[S,A,C]`, derive D/E/F).

- **Action for brainstorm spec:** Lift the **corrected design from `ms-v0-2-spec-R0-review.md` C2/I2/I3/M5** (NOT §5 invariants 1/2 verbatim). Amend `SPEC_ms_v0_1.md §5` **and** `MIGRATION.md` invariants 1+2 to the threshold-field mechanism; add the prefix-byte registry note (`0x01` = unallocated, not entr-share). Cite source SHA `9d789b2` + codex32 `0.1.0`. Re-grep all codex32 `lib.rs` line numbers at spec-write time (the R0's citations are `4e5266a`-era snapshots).

---

## Cross-cutting observations

1. **The hard part is already done.** `ms-v0-2-spec-R0-review.md` is effectively a deep, codex32-source-verified pre-recon of K-of-N: C2 (the threshold-field pivot), I2 (`combine_shares` routes through `discriminate()`/`validate()`; surface `MismatchedLength`+`MismatchedHrp`, both reachable from `interpolate_at` `lib.rs:239,242` but omitted from §3/§6), I3 (`combine_shares` pre-validates distinct indices + count ≥ k — `interpolate_at`'s checks are lazy), M5 (**no public `derive_share`** — `encode_shares` derives all N internally; a public per-share derive invites the re-stamp footgun + the misuse-deriving-S footgun), M3 (`Threshold::ZERO` is a **const**, `Threshold::new` = 2..=9), M2 (`ms split` emits the whole N-share set → **private-key-material** output class; `ms combine` + decode/inspect same). The math is **affirmed sound** (secret@S + k−1 random defining points, interpolate to derive the rest, recover at S; matches BIP-93 + `bip_vector_3`).
2. **Two STRUCTURALLY-WRONG citations** (§5/MIGRATION inv 1+2) — both crypto, both already corrected in the prior R0. No DRIFTED-by-N line moves of note; the substrate is clean.
3. **OPEN scope question for the brainstorm — can `mnem` secrets be shared, or entr-only?** The prior R0 scoped "mnem is never shared in v0.2" (mnem stays threshold-0 single-string). Mechanically mnem *could* be shared now (secret-at-S payload `[0x02][lang][entropy]`, recovered + read by `discriminate()` as mnem) since sharing is keyed on the threshold header, orthogonal to the prefix byte. Decide explicitly: **entr-only (simpler, matches the prior R0)** vs **entr+mnem (uniform; the language survives the split)**. `seed`/`xprv` framing stays **OUT** (don't fit BIP-93 brackets — §5 closing para).
4. **No combined `SPEC_ms_v0_2.md` draft survives** — the split kept only the mnem half. Cycle 2 authors a fresh SPEC (`SPEC_ms_v0_2_kofn.md` or similar) from §5 + MIGRATION.md + the R0 corrections.
5. **SemVer / breaking?** NOT breaking the v0.1 contract: v0.1 entr (+ v0.2 mnem) single-strings stay readable; K-of-N shares (threshold∈2..9) are a **new** wire shape a v0.1 decoder correctly rejects (v0.1 never emitted shares). The change *relaxes* the decoder's threshold-0-only reject into a route → **additive, MINOR**: ms-codec 0.3.0→**0.4.0**, ms-cli 0.6.0→**0.7.0**.

---

## Recommended brainstorm-session scope

- **One cycle, LARGE, heaviest lockstep in the constellation.** This is the format's defining feature; budget a multi-phase cycle (the prior R0 affirms the math but the wire/API needs the C2 re-derivation written into a fresh SPEC).
- **Phasing (mirror the mnem cycle's gate discipline):** P0 spike (prove `encode_shares(tag, ZERO, &[p])` byte-identical to `encode` via the SHA-pinned v0.1 vectors **+** a K-of-N round-trip against codex32 `bip_vector_3` — the hard gate before any wire commit) → P1 codec (`Threshold` type w/ `ZERO` const, `encode_shares` deriving all N internally, `combine_shares` via `interpolate_at(_,Fe::S)` with the I2/I3 pre-validation, new error variants `InvalidThreshold`/`Mismatched*`/`RepeatedIndex`/`ThresholdNotPassed` alphabetical-inserted, **threshold-keyed decode dispatch**, §5+MIGRATION.md amendment) → P2 ms-cli (`ms split`/`ms combine` + output-class + reuse `codex32_friendly.rs`) → P3 lockstep.
- **SemVer:** MINOR both crates (ms-codec 0.4.0 / ms-cli 0.7.0).
- **Locksteps (REAL `schema_mirror` this time, unlike mnem):** (a) GUI `mnemonic-gui/src/schema/ms.rs` — **two NEW subcommands** `split`/`combine` ⇒ genuine flag-NAME `schema_mirror` lockstep (gated; `gui-schema` is reflective so they auto-appear in the emitted schema, the hand-mirror must catch up). (b) `ms decode`/`inspect` `--json` gains a share shape — **NOT** schema_mirror-gated (wire-shape); paired-PR manual update. (c) manual `docs/manual/src/40-cli-reference/43-ms.md`. (d) toolkit re-pin ms-codec 0.3.0→0.4.0 (crates.io) + ms-cli v0.6.0→v0.7.0 (tag, `install.sh` + `manual.yml`); toolkit `inspect`/`decode`/`repair` must dispatch shares on the **threshold field**, not `0x01` — and decide whether the toolkit gains a consume/combine surface or just re-pins (brainstorm scope call). (e) `ms-codec-no-ci-workflow` FOLLOWUP still open → **local verification is the only gate** (full suite + clippy at every phase; no fmt gate).
- **Mandatory next gate:** brainstorm spec → opus architect **R0, 0C/0I before any code** (re-dispatch after every fold). The C2 invariant rewrite + the I3 lazy-validation are drift-prone — expect ≥2 rounds.
